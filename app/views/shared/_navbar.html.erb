<nav class="navbar navbar-expand-lg" style="">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="/home">
      AlcoMarket</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav container-fluid justify-content-center">
        <a class="nav-link text-warning<%= 'active text-white' if current_page?(home_path) %>" href="/home">Home</a>
        <a class="nav-link text-warning<%= 'active text-white' if current_page?(categories_path) %>" href="/categories">Menu</a>
        <% if Current.user && Current.user.role != 'admin' %>
          <a class="nav-link text-warning<%= 'active text-white' if current_page?(profile_edit_path) %>" href="/profile/edit">Profile</a>
          <a class="nav-link text-warning<%= 'active text-white' if current_page?(orders_path) %>" href="/orders">Orders</a>
        <% end %>
        <% if Current.user && Current.user.admin? %>
          <a class="nav-link text-warning <%= 'active text-white' if current_page?(orders_path) %>" href="/orders">Orders</a>
          <a class="nav-link text-warning<%= 'active text-white' if current_page?(new_menu_path) %>" href="/menu/new">Add Alcohol</a>
        <% end %>
      </div>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 gap-1 ">
        <% if Current.user %>
          <% if Current.user.role != 'admin' %>
            <a class="navbar-brand text-warninig" href="/cart">
              <img src="https://cdn-icons-png.freepik.com/512/275/275790.png" alt="Bootstrap" width="30" height="30">
            </a>
          <% end %>
          <li class="nav-item">
            <%= link_to Current.user.email, edit_password_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-warning" %>
          </li>
        <% else %>
          <a class="navbar-brand" href="/cart">
            <img src="https://cdn-icons-png.freepik.com/512/275/275790.png" alt="Bootstrap" width="30" height="30">
          </a>
          <li class="nav-item d-flex justify-content-center align-items-center">
            <%= link_to "Login", sign_in_path, class: "btn btn-outline-warning mx-auto btn-block" %>
          </li>
          <br>
          <li class="nav-item d-flex justify-content-center align-items-center">
            <%= link_to "Sing Up", sign_up_path, class: "btn btn-outline-warning ", style: "width: 90px;" %>
          </li>
          <br>
        <% end %>
      </ul>

    </div>
  </div>
</nav>